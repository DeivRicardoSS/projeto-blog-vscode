import mongoose from 'mongoose';

const RespostaSchema = new mongoose.Schema({
    id: {
        type: String,
        required: true
    },
    id_pai: {
        
    },
    id_dono: {

    },
    conteudo: {

    }
})